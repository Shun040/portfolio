# 设计规范 — 尹紫涔作品集 2026

PC / 移动共用一套。令牌写在 `css/site.css`（排印、栅格、间距）和
`css/bento.css`（极光、毛玻璃、圆角）顶部的 `:root` 里，改这里就是改全站。

---

## 1. 设计立场

**暗色极光底 + 毛玻璃面板 + 便当盒栅格。**

科技感来自三件事：一层缓慢呼吸的极光、玻璃的厚度（边缘高光 + 背景模糊）、
以及大小不等的便当格所形成的节奏。避开通用模板站的方式不是"不用效果"，
而是**每个效果都有具体职责**：

| 元素 | 职责 |
|---|---|
| 极光渐变 | 全站唯一的色彩来源，内容区一律中性灰 |
| 毛玻璃 | 建立层级 —— 浮在极光之上的是内容，沉在下面的是氛围 |
| 便当盒 | 用格子大小表达重要性，毕业作品占最大格 |
| 骨架屏 | 消除"先空白再砸图"的廉价感 |
| 噪点层 | 压住渐变色带（banding），这是高级感的关键一步 |

---

## 2. 色彩

### 底层中性色（`site.css`）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--bg` | `#08090B` | 页面底色 |
| `--fg` | `#EDEEF1` | 正文主色 |
| `--fg-2` | `#9AA1AD` | 次级正文 |
| `--fg-3` | `#656C79` | 单宽标签、注释 |
| `--accent` | `#7C8CFF` | 中性强调 |

### 极光与玻璃（`bento.css`）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--au-1` | `#6E7BFF` 靛蓝 | 左上光斑 |
| `--au-2` | `#34E3D0` 青 | 右上光斑、数字渐变 |
| `--au-3` | `#C86DD7` 品红 | 左下光斑 |
| `--au-4` | `#2B6CF0` 深蓝 | 右下光斑 |
| `--glass` | `rgba(255,255,255,.045)` | 玻璃底 |
| `--glass-brd` | `rgba(255,255,255,.11)` | 玻璃边 |
| `--blur` | `20px` | 背景模糊半径 |

**每个项目自带一对主色**（`data.js` 里的 `a` / `b`）。打开案例时写进
`--pa` / `--pa-solid`，案例内的条形图、序号、引述边线、成果数字全部跟着变 ——
六个案例是六种色温，但都在同一套玻璃语言里。

对比度：`--fg` on `--bg` ≈ 16:1，`--fg-2` ≈ 7.4:1，`--fg-3` ≈ 4.6:1（AA）。

---

## 3. 字体

**不引入任何外部字体。** 全部系统栈 —— 本地预览秒开，离线可用，
中文自动落到苹方 / 微软雅黑，不会有字体加载闪烁。

- 无衬线 `--sans`：`-apple-system, "PingFang SC", "Microsoft YaHei", …`
- 单宽 `--mono`：`ui-monospace, SFMono-Regular, Menlo, …`

### 字阶（流体，PC 与移动共用）

| 令牌 | clamp | 移动 → 桌面 |
|---|---|---|
| `--t-display` | `clamp(2.6rem, 8.4vw, 7.5rem)` | 42 → 120px |
| `--t-h1` | `clamp(2rem, 4.6vw, 3.9rem)` | 32 → 62px |
| `--t-h2` | `clamp(1.5rem, 2.8vw, 2.4rem)` | 24 → 38px |
| `--t-body` | `clamp(.95rem, .55vw + .82rem, 1.075rem)` | 15 → 17px |
| `--t-meta` | `11px` 固定 | 所有单宽标签 |

用 `clamp()` 意味着**不需要为移动端再写一套字号表**，断点只处理布局。

### 排印规则

- 大标题：`line-height: .94`、`letter-spacing: -.035em`、字重 500（不用 700）
- 单宽标签：11px / 大写 / `letter-spacing: .16em`
- 中文语境（`html[data-lang="zh"]`）自动把行高提到 1.78、字距收到 `.1em`
- 大数字用 `background-clip: text` 走白 → 青渐变，是全站唯一的文字渐变
- 正文最大宽度 `46–62ch`

---

## 4. 便当盒栅格

12 列。每个盒子用三个自定义属性控制，加减盒子不用动栅格定义：

```html
<article class="box glass" style="--c:8;--rw:2;--mh:420px">
```

| 属性 | 含义 |
|---|---|
| `--c` | 占几列（1–12） |
| `--rw` | 占几行 |
| `--mh` | 最小高度 |

### 项目格的尺寸分配

| # | 项目 | 列 × 行 |
|---|---|---|
| 01 | Tears before Words | 8 × 2（主位） |
| 02 | EXSTASIS | 4 × 1 |
| 03 | PainShift | 4 × 1 |
| 04–06 | Reground / EmpaLens / Through Their Eyes | 4 × 1 |

**格子大小 = 重要性**。毕业作品占掉第一屏的三分之二。

响应：`≤1000px` 栅格降到 6 列（`--c` 被 `min()` 夹住），`≤640px` 全部单列。

---

## 5. 毛玻璃

```css
.glass {
  background: rgba(255,255,255,.045);
  border: 1px solid rgba(255,255,255,.11);
  border-radius: 20px;
  backdrop-filter: blur(20px) saturate(160%);
}
```

关键在 `::before` 的**边缘高光**：一条从左上到右下衰减的白线，
用 `mask-composite: exclude` 只保留 1px 描边。没有这一层，
玻璃看起来是一块半透明塑料；有了它才有厚度。

不支持 `backdrop-filter` 的浏览器退回 `rgba(14,16,22,.92)` 实心底。

---

## 6. 极光背景

四团径向渐变，`filter: blur(90px)`，各自 34 / 42 / 38 / 46 秒的周期缓慢漂移
（`alternate`，永不回到同一帧）。上面盖一层 SVG `feTurbulence` 噪点，
`opacity: .035`，专门用来压渐变的色带。

固定定位、`z-index: 0`、`pointer-events: none`，内容用
`body > *:not(.aurora) { position: relative; z-index: 1 }` 浮在它上面。

`prefers-reduced-motion` 下光斑停止漂移，保留静态渐变。

---

## 7. 骨架屏 / 占位加载

所有图片和视频都包在 `.ph` 里：

- 未就绪：`rgba(255,255,255,.035)` 底 + 1.5 秒循环扫光
- 就绪：JS 打上 `.ready`，扫光停掉、媒体 0.7 秒淡入
- **出错也打 `.ready`** —— 否则加载失败的图会永远在闪

人物图 `assets/portrait.jpg` 缺失时 `onerror` 加 `.noimg .ready`，
露出斜纹占位块，不会裂图。

---

## 8. 交互技法分配

每个技法只解决一个具体的表达问题，不叠在同一个模块上。

| 技法 | 用在哪 | 承担的表达 |
|---|---|---|
| 交互式流体扭曲 | 开场页 | 场绕着名字流动 |
| 逐字错峰入场 | 各级标题 | 标题"被写出来" |
| 便当盒 | 经历 / 项目 / 页尾 | 用格子大小表达重要性 |
| 可交互案例浮层 | 项目详情 | 把作品集变成能操作的东西 |
| 3D 环形轮播 | 个人优势 | 六个面 = 六条能力 |

### 8a. 流体点阵场（`js/field.js`）

自研 canvas 引擎。粗网格半拉格朗日流体（平流 → 扩散 → 衰减），
分辨率就是点阵分辨率（约 120×75 ≈ 9000 格）。纯 JS 稳在 60fps，不用 WebGL。

**名字不会被搅散。** `distortText: false` 时，属于名字的格子位移量乘以
`1 - mask × 0.88`，几乎钉死不动；`baseReveal: 0.62` 保证不靠光标也读得清。
结果是流体绕着字流过去，字始终清晰 —— 而不是一搅就糊。

性能：alpha 量化成 7 档，每档一次 `beginPath / fill`（8 次绘制调用代替上万次）；
低于阈值的点直接跳过；DPR 上限 2；速度低于阈值直接归零。

### 8b. 逐字错峰入场

只拆标题（`.split`）。正文不拆 —— 整段逐字跳动读不下去，
而且会把一个段落变成上百个 `inline-block`。

递归遍历子节点，`<em>` 和 `<br>` 原样保留，空格不包 span。
每字延迟 26ms，`translateY(.52em) rotateX(-52deg)` → 归位。
语言切换会重写 `innerHTML`，所以切换后重拆一次，已入场的标题强制保持可见。

### 8c. 3D 环形轮播

六张能力卡贴在圆柱六个面上，滚动进度驱动绕 Y 轴旋转。
半径 `R = (卡宽/2) / tan(π/6) × 1.12`，JS 按实际宽度算，保证不穿插。
每面透明度 = `0.12 + 0.88 × max(0, cos(朝向角))`，背面自然退到底色里。

---

## 9. 案例浮层的可视化规范

案例不是一句话简介，是七段结构：
**问题 → 调研 → 机制 → 系统 → 体验 → 成果 → 影像**。

### 调研图表（`research.type`）

| 类型 | 形态 | 用在 |
|---|---|---|
| `bars` | 横条，宽度由 `--v` 驱动，打开时生长 | PainShift · Reground |
| `donut` | `conic-gradient` 环 + 中心数字 | Through Their Eyes |
| `timeline` | 三段式对照表 | Tears · EXSTASIS |
| `bandscale` | 量表分段色带 | EmpaLens |

条形图靠 `.case.shown` 延迟一帧才加类，否则会直接跳到终点没有动画。

### 机制模块（`concept.type`）

| 类型 | 交互 | 用在 |
|---|---|---|
| `mapping` | 点一条亮一条的左右映射 | Tears · EXSTASIS |
| `loop` | 循环步骤，1.8 秒自动轮转，可点跳转 | PainShift · Through Their Eyes |
| `states` | 状态切换，换图 / 换 emoji / 换色 | Reground · EmpaLens |

### 专属可玩模块

- **PainShift 曼陀罗宫格** —— 10 段实拍视频，`preload="none"`，
  悬停播放、点击锁定。这是呼吸驱动的 TouchDesigner 实际输出
- **PainShift 呼吸练习** —— 4 秒吸 / 6 秒呼，每完成一次呼吸会话深度 +14%，
  模拟心率与用药依赖随之下降。把项目的生物反馈逻辑直接搬进浏览器

### 视频

YouTube 一律用**外观占位**（facade）：先显示缩略图，点击才插入 iframe。
六个项目一次性嵌十个 iframe 会把首屏拖垮。缩略图取不到时回落到项目主图。

---

## 10. 动效

| 参数 | 值 |
|---|---|
| 缓动 `--ease` | `cubic-bezier(.22,.61,.36,1)` |
| 常规时长 | `.45s` |
| 微交互 | `.25s – .35s` |
| 极光漂移 | 34 – 46s |
| 环形轮播 | 由滚动进度直接驱动 |

hover 只用三种表达：玻璃底变亮、边框提亮、图片轻微放大。不用投影，不用位移。

`prefers-reduced-motion: reduce` 下：动画时长压到 0.001ms、极光停止漂移、
点阵场只渲染一帧静态图、环形轮播降级为便当盒网格、条形图不做生长动画、
循环步骤不自动轮转、呼吸练习不启动。

---

## 11. 降级矩阵

| 条件 | 项目 | 优势 | 开场 |
|---|---|---|---|
| ≥1000px | 便当盒 + 案例浮层 | 3D 环形 | 流体 |
| < 1000px | 便当盒（6 列） | 便当盒网格 | 流体 |
| < 640px | 便当盒（单列） | 便当盒网格 | 流体 |
| `reduce` | 便当盒 + 案例浮层 | 便当盒网格 | 静态一帧 |

跨越 1000px 断点时页面 `location.reload()` —— 两个容器已删掉一个，
重载最省事也最不易出错。

---

## 12. 可访问性

- 焦点环：`1px solid var(--accent)`，`outline-offset: 3px`
- 跳转链接 `.skip`，Tab 第一下就能跳到正文
- 案例浮层：`role="dialog"` + `aria-modal`，Esc 关闭，打开时锁定页面滚动，
  关闭后滚回原位置，焦点落在关闭按钮
- 机制模块的按钮带 `aria-pressed` / `aria-current`
- 汉堡菜单带 `aria-expanded`，导航当前项带 `aria-current`
- 装饰性元素（canvas、极光、序号、箭头）一律 `aria-hidden`
- 图片 `alt` 走 i18n，随语言切换
- 案例支持深链接（`work.html#painshift`），可以直接把某个项目发给别人
