# 设计规范 — 尹紫涔

参考 [bamlab.ch](https://bamlab.ch/en/) 的系统重做。令牌全部写在 `css/archive.css` 顶部的 `:root`。

---

## 1. 立场

不是深色模板站，也不是纸质档案。是**暖奶油字压在近黑底上**的编辑式版面 ——
瑞士工作室那一路：字大、线细、留白狠、颜色只有一个。

刻意不用：毛玻璃、圆角、渐变色块、投影、极光、药丸按钮。

---

## 2. 色彩

| 令牌 | 值 | 说明 |
|---|---|---|
| `--bg-deep` | `#060812` | 底色。**带蓝的近黑，不是纯黑** |
| `--bg` / `--bg-2` | `#0C0E16` / `#12151F` | 次级底 |
| `--fg` | `#ECE7D4` | **暖奶油，不是白** —— 整套高级感的来源 |
| `--fg-dim` | `rgba(236,231,212,.55)` | 次级正文 |
| `--fg-faint` | `rgba(236,231,212,.30)` | 单宽标签 |
| `--rule` | `rgba(236,231,212,.14)` | 发丝线。是奶油色的，不是灰 |
| `--red` | `#C5283D` | **唯一强调色** |

红只出现在四个地方：区块序号、`ARTIST` 身份行、法式引号、当前导航项前的点。
其余一律奶油灰阶。

---

## 3. 字体（全部本地，不走 CDN）

`assets/fonts/`，共 164KB：

| 用途 | 字体 | 权重 |
|---|---|---|
| 标题 / 正文 | **Switzer** | 400 / 500 / 600 |
| 元信息 | **IBM Plex Mono** | 400 / 500 |
| 中文 | Switzer 打头，落到 PingFang SC / 微软雅黑 | — |

Switzer 来自 Fontshare，Plex Mono 来自 IBM 官方仓库，都可免费用于网页。

### 字距随字号变

| | letter-spacing |
|---|---|
| `.cover-name`（最大） | `-.04em` |
| `.display` | `-.032em` |
| `.h2` / 作品名 | `-.02em` ~ `-.028em` |
| 正文 | `0` |
| `.mono` 单宽标签 | `+.14em`，大写 |

中文语境下负字距全部收回（`html[data-lang="zh"]` 覆盖）—— 中文不需要收紧。

---

## 4. 间距与缓动

全部 clamp，PC 与移动共用一套：

```
--xs .5→.75rem   --sm .75→1.25rem   --md 1.25→2rem
--lg 2→4rem      --xl 3→8rem        --gutter 1.25→4rem
--measure 64ch   --measure-narrow 48ch   --maxw 1280px
```

缓动只有一条：`--ease: cubic-bezier(.16, 1, .3, 1)` —— 起步快、收尾极慢。
时长三档：`.18s / .32s / .6s`。

---

## 5. 作品：编号列表

不是卡片墙，不是便当盒。每个作品一行：

```
01 · 交互装置 · 生物艺术 · 2024
EXSTASIS
« 在当代社会，技术对自然的过度索取…… »
（PDF 原文的一句说明）                          调阅 →
```

- 序号红色，`·` 分隔，全大写单宽
- 作品名 clamp(1.6rem, 3.4vw, 2.6rem)
- 法式引号 `«»` 由 CSS `::before/::after` 生成，引号是红的
- hover 时整行背景亮 3%（向左右溢出到 gutter 外），右侧「调阅 →」右移并变红
- 列表下方是六格图版，默认去色，hover 恢复彩色并放大 4%

---

## 6. 案例浮层

- 弹簧驱动（`js/spring.js`，60 行，无外部库），`damping .85 / response .34`
- 从被点的那一行长出来（`transform-origin` 锚定），关闭原路返回
- **可打断**：展开中途点关闭会从当前位置接住反向，不会先播完
- 条形图轨道压到 2px，填充纯红 —— 不用渐变

---

## 7. 无障碍

- `prefers-reduced-motion` —— 动画压到 0.001ms，条形图不生长
- `prefers-reduced-transparency` —— 页眉去掉毛玻璃改实心
- `prefers-contrast: more` —— 次级文字与线全部提亮
- 焦点环 `1px solid var(--red)`，offset 3px
- 按下即反馈：所有可点元素 `:active` 缩放或降透明度，`touch-action: manipulation` 去掉移动端 300ms 延迟

---

## 8. 开场页

同一套配色。奶油色墨点铺满视口，名字写在点阵里，
光标搅动**名字周围的场**（`distortText: false`），名字本身始终清晰。
引擎见 `js/field.js`（粗网格半拉格朗日流体，纯 JS，约 9000 格）。
